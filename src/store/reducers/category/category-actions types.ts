const CategoryActionsTypes = {
    fetchStart: "categories/fetchStart",
    fetchSuccess: "categories/fetchSuccess",
    fetchFailure: "categories/fetchFailure",
} as const;

export default CategoryActionsTypes;
